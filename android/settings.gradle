pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

plugins {
    // This allows Gradle to automatically download the required JDK 21
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}

include ':app'
