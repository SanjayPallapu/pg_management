name: Build Android App Bundle (AAB)

on:
  push:
    branches:
      - main
      - early-access
  workflow_dispatch:
    inputs:
      release_action:
        description: Choose whether to build only or upload a draft to the Google Play alpha track
        required: true
        default: build-only
        type: choice
        options:
          - build-only
          - upload-alpha-draft

permissions:
  contents: read

concurrency:
  group: android-aab-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    name: Build & Sign AAB
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Setup Java JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install Dependencies
        run: npm ci

      - name: Run Unit Tests
        run: npm test

      - name: Build Web Assets
        run: npm run build

      - name: Sync Capacitor
        run: npx cap sync android

      - name: Validate Android Signing Secrets
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: |
          missing_secrets=0

          if [ -z "$KEYSTORE_BASE64" ]; then
            echo "::error::Missing GitHub secret: KEYSTORE_BASE64"
            missing_secrets=1
          fi
          if [ -z "$KEYSTORE_PASSWORD" ]; then
            echo "::error::Missing GitHub secret: KEYSTORE_PASSWORD"
            missing_secrets=1
          fi
          if [ -z "$KEY_ALIAS" ]; then
            echo "::error::Missing GitHub secret: KEY_ALIAS"
            missing_secrets=1
          fi
          if [ -z "$KEY_PASSWORD" ]; then
            echo "::error::Missing GitHub secret: KEY_PASSWORD"
            missing_secrets=1
          fi

          if [ "$missing_secrets" -ne 0 ]; then
            exit 1
          fi

      - name: Decode Keystore
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
        run: |
          printf '%s' "$KEYSTORE_BASE64" | base64 --decode > android/app/release.keystore
          echo "Keystore successfully decoded."

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build AAB
        env:
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: |
          cd android
          chmod +x gradlew
          ./gradlew bundleRelease

      - name: Upload Release AAB Artifact
        uses: actions/upload-artifact@v4
        with:
          name: PG-HUB-AAB-${{ github.run_number }}-${{ github.sha }}
          path: android/app/build/outputs/bundle/release/app-release.aab
          if-no-files-found: error
          retention-days: 14

      - name: Write Workflow Summary
        if: ${{ always() }}
        run: |
          {
            echo "## PG HUB Android AAB"
            echo ""
            echo "- Commit: \`${GITHUB_SHA}\`"
            echo "- Branch: \`${GITHUB_REF_NAME}\`"
            echo "- Run number: \`${GITHUB_RUN_NUMBER}\`"
            if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ "${{ inputs.release_action }}" = "upload-alpha-draft" ]; then
              echo "- Requested action: upload an alpha-track draft"
            else
              echo "- Requested action: build only"
            fi
          } >> "$GITHUB_STEP_SUMMARY"

  upload-alpha-draft:
    name: Upload Alpha Draft to Google Play
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.release_action == 'upload-alpha-draft' }}
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment:
      name: google-play-alpha

    steps:
      - name: Download Signed AAB
        uses: actions/download-artifact@v4
        with:
          name: PG-HUB-AAB-${{ github.run_number }}-${{ github.sha }}
          path: release

      - name: Validate Google Play Secret
        env:
          GOOGLE_PLAY_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_PLAY_SERVICE_ACCOUNT_JSON }}
        run: |
          if [ -z "$GOOGLE_PLAY_SERVICE_ACCOUNT_JSON" ]; then
            echo "::error::Missing GitHub secret: GOOGLE_PLAY_SERVICE_ACCOUNT_JSON"
            exit 1
          fi

      - name: Upload Draft to Google Play Alpha
        uses: r0adkll/upload-google-play@v1
        with:
          serviceAccountJsonPlainText: ${{ secrets.GOOGLE_PLAY_SERVICE_ACCOUNT_JSON }}
          packageName: com.sanjay.pgmanagement
          releaseFiles: release/app-release.aab
          tracks: alpha
          status: draft
          releaseName: PG HUB ${{ github.sha }}

      - name: Write Upload Summary
        run: |
          {
            echo "## Google Play upload"
            echo ""
            echo "- Track: \`alpha\`"
            echo "- Status: \`draft\`"
            echo "- Commit: \`${GITHUB_SHA}\`"
            echo ""
            echo "The draft must still be reviewed and released in Google Play Console."
          } >> "$GITHUB_STEP_SUMMARY"
